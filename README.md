# URL Shortener - Spring Boot

A basic URL Shortener built using Spring Boot for learning purpose
Open to suggestions.


clone it and run it with 
Mac/Linux:
./mvnw spring-boot:run

Windows:
mvnw.cmd spring-boot:run
